<% include ../partials/header %>
<div class="container">
	<header class="jumbotron">
		<div class="container">
			<h1>Welcome to Nepal</h1>
			<p>Explore</p>
			<p>
				<a class = "btn btn-primary btn-large" href="/campgrounds/new">Add New Place</a>
			</p>
		</div>
	</header>

	<div class="row text-center" style="display:flex;flex-wrap: wrap;">
	<% camp.forEach(function(camp){ %>
		<div class='col-md-3 col-sm-6'>
			<div class='thumbnail'>
			<a href="/campgrounds/<%=camp.id%>">
				<img src=<%=camp.image%>>
				<div class="caption">
					<h4><%=camp.name%></h4>
					<h4><%=camp.views%></h4>
				</div>
			</a>
			</div>
		</div>
	<%});%>
	</div>
<% include ../partials/footer %>
</div>